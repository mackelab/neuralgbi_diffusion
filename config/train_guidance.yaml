# main.yaml
defaults:
  - _self_
  - hydra: hydra
  - dataset: dataset
  - model: guidance_model 
  - diffusion: diffusion
  - optimizer: optimizer

data_entity: gaussian_mixture
check_val_every_n_epochs: 5
num_worker: 8

max_epochs: 100 
batch_size: 256
precision: 32  # 64: extended, 32: standard, 16: half
