from pathlib import Path
from typing import List
from gbi_diff.scripts.sampling import diffusion_sampling


def evaluate_diffusion_sampling(
    diffusion_ckpt: str | Path, guidance_ckpt: str | Path, eval_config: object
):
    pass
